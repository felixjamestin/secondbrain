const CONFIG = {
  CACHE_TIMEOUT: 7, // No of days after which data from Airtable is refetched
  LOGGING_ENABLED: true, // Toggle logging calls inside the app
  ENTRY_DISPLAY_MODE: "random" // Accepted values are "RANDOM" or "SEQUENTIAL". This controls how the entries are fetched
};

export { CONFIG };
